#include <vector>
#include <cassert>
#include <string>
#include <set>
#include <map>
#include <algorithm>

using namespace std;

class LRUCache {
public:
    LRUCache(int capacity) {
        
        keyValue.reserve(capacity);
    }
    
    int get(int key) {
        
    }
    
    void put(int key, int value) {
        
    }
    vector<pair<int,int>> keyValue;
};
